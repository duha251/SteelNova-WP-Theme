<?php
$content = $item['content'] ?? $settings['content'];
$author_name = $item['author_name'] ?? $settings['author_name'];
$author_title = $item['author_title'] ?? $settings['author_title'];
$author_image = $item['author_image'] ?? $settings['author_image'];
$rating  = $item['rating'] ?? $settings['rating'];
$rating_label = $settings['rating_label'];
$rating_icon = $settings['rating_icon'];
$icon = $settings['icon'];
?>

